/*
for (String palavra : palavrasParaFlashcard) {
            Flashcard flashcard = new Flashcard();
            flashcard.setUser(usuario);
            flashcard.setWord(palavra);
            flashcard.setInterval(1); // Começa com intervalo de 1 dia para revisão
            flashcard.setNextReviewDate(LocalDate.now().plusDays(1));
            flashcard.setEaseFactor(2.5f); // Fator padrão do algoritmo SuperMemo (SM-2)
            flashcard.setLastQuality(null); // Sem avaliação inicial
            flashcard.setCreatedAt(LocalDateTime.now());

            this.flashcardRepository.save(flashcard);
        }
*/
